<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalproject_bicycle">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalProjectBicycle">
        <activity android:name=".rear"></activity>
        <activity android:name=".adjust" />
        <activity android:name=".pedal" />
        <activity android:name=".backs" />
        <activity android:name=".front" />
        <activity android:name=".tube" />
        <activity android:name=".installingw" />
        <activity android:name=".repair" />
        <activity android:name=".type" />
        <activity android:name=".access" />
        <activity android:name=".Parts" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>